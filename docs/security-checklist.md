# 安全审查清单

> **审查对象：** `成员代码/xiezhizhuo/Client.py`（整改前后均需填写）  
> **审查人：** renyanbin  
> **审查日期：** 2026-06-25

---

## 第一层审查：对照 Prompt 约束检查

> 验证 AI 生成代码是否真正落实了约束，而非只在注释里写了但代码逻辑没改

| 约束条目 | 要求 | 代码中是否实际落实 | 落实位置（行号/函数名） |
|----------|------|-------------------|----------------------|
| 路径规范化 | 使用 `os.path.realpath()` | ✅ 是 | `validate_filename()` 第 18 行 |
| OUTPUT_DIR 边界检查 | `startswith(OUTPUT_DIR)` | ✅ 是 | `validate_filename()` 第 20 行 |
| with 语句写文件 | 不能用裸 `open().write()` | ✅ 是 | `receive_file()` 第 52 行 |
| 文件大小限制 | 累计字节 > MAX_FILE_SIZE 则中止 | ✅ 是 | `receive_file()` 接收循环第 61 行 |
| 扩展名白名单 | 非白名单扩展名拒绝写入 | ✅ 是 | `receive_file()` 第 48 行 |
| 具体异常类型 | 不能用裸 `except Exception` | ✅ 是 | 所有 except 块已具体化 |
| 无第三方库 | 只用标准库 | ✅ 是 | import 清单无第三方包 |
| 功能完整性 | 原有 TCP 连接逻辑不变 | ✅ 是 | main() 流程与原版一致 |

---

## 第二层审查：人工业务逻辑审查

### 认证与授权

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 连接前是否有身份验证 | ❌ 无 | TCP 客户端本身无认证机制，属于已知设计局限 |
| 文件访问是否有权限控制 | ⚠️ 部分 | 通过扩展名白名单做了粗粒度控制 |
| 是否遵循最小权限原则 | ✅ 是 | OUTPUT_DIR 限制了写入范围 |

### 错误响应安全性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 错误信息是否向用户泄露内部路径 | ✅ 修复后已消除 | 原代码 print 了完整路径，整改后改为模糊提示 |
| 异常是否被静默吞掉 | ✅ 修复后已消除 | 安全相关异常改为 raise 并记录 |
| 连接失败是否暴露服务器信息 | ⚠️ 轻微 | 仍会打印 IP:Port，属于低风险 |

### 数据存取安全性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 文件写入路径是否受控 | ✅ 是 | realpath + OUTPUT_DIR 双重校验 |
| 写入文件是否存在覆盖风险 | ⚠️ 存在 | 若同名文件已存在则直接覆盖，整改说明见 fix-report.md |
| 大文件拒绝服务防护 | ✅ 是 | 100MB 限制已添加 |

---

## 总体审查结论

- **P0 问题（整改前）：** 路径穿越漏洞 — 已修复 ✅
- **P1 问题（整改前）：** 接收内容无限制 — 已修复 ✅  
- **P1 问题（整改前）：** 裸异常捕获 — 已修复 ✅
- **遗留可接受风险：** 明文传输（R-02）— 本次标注警告注释，不纳入整改范围（生产环境应加 TLS）
- **新发现问题：** 同名文件覆盖（P2）— 已在整改报告中说明处置方案

**审查结论：** 整改后代码满足本次安全加固目标，核心路径穿越风险已消除，可提交 PR。

---

*本审查清单在 AI 生成代码后由 renyanbin 手工填写，结合代码 diff 逐行核对。*



---

# 安全审查清单

> **审查对象：** `成员代码/renyanbin/amia_defense_test.py`（整改后版本）  
> **审查人：** 谢智卓  
> **审查日期：** 2026-06-26

---

## 第一层审查：对照项目约束文档检查

> 验证 AI 生成代码是否真正落实了约束文档中 R‑01 ~ R‑08 的要求，而非只在注释里写了但代码逻辑没改

| 约束条目（风险编号） | 具体要求 | 代码中是否实际落实 | 落实位置（行号/函数名） |
|----------------------|----------|-------------------|------------------------|
| R-01 路径规范化 | 使用 `os.path.realpath()` 规范化从 JSON 读取的 `img_path` | ✅ 是 | `safe_join_and_validate()` 第 75‑82 行 |
| R-01 边界检查 | 验证最终绝对路径在指定的安全根目录（`data/`）内 | ✅ 是 | `safe_join_and_validate()` 第 79‑80 行（`startswith` 检查） |
| R-01 主数据文件校验 | 对 `DATA_INPUT` 同样做路径规范化校验 | ✅ 是 | `load_data()` 第 123‑125 行 |
| R-02 模块路径污染 | 移除 `sys.path.append("../../")`，改用基于绝对路径的导入 | ✅ 是 | 第 21‑34 行（计算 `_PROJECT_ROOT` 并验证 `amia.py` 存在） |
| R-02 路径校验 | 验证项目根目录包含关键模块后再添加至 `sys.path` | ✅ 是 | 第 28‑31 行（`os.path.isfile(_AMIA_PATH)` 检查） |
| R-03 Prompt 注入防护 | 限制输入提示词长度，超长截断或跳过 | ✅ 是 | 第 176‑180 行（`MAX_PROMPT_LENGTH=4096`，截断并记录警告） |
| R-04 资源耗尽（图片像素） | 校验图片像素尺寸，设置 `MAX_IMAGE_PIXELS = 16MP` | ✅ 是 | 第 205‑207 行（检查 `width*height`） |
| R-04 资源耗尽（文件大小） | 校验图片文件大小，设置 `MAX_IMAGE_FILE_SIZE = 50MB` | ✅ 是 | 第 198‑200 行（`os.path.getsize` 检查） |
| R-04 单条日志大小限制 | 限制单条日志记录大小（`MAX_LOG_ENTRY_SIZE = 10KB`） | ✅ 是 | `safe_write_log_entry()` 第 99‑101 行（`len(json_str.encode())` 检查） |
| R-05 异常处理 | 捕获具体异常类型（`FileNotFoundError`, `ValueError`, `OSError`, `Image.UnidentifiedImageError`, `torch.cuda.OutOfMemoryError` 等） | ✅ 是 | 图片加载第 202‑213 行；推理第 218‑227 行；评估第 230‑234 行；写入第 244‑247 行 |
| R-05 裸异常禁止 | 不使用裸 `except Exception` 而不记录 | ✅ 是 | 所有 `except` 块均有具体异常类型，仅最后一层 `except Exception` 用于兜底并记录日志 |
| R-06 日志脱敏 | 对 `output`、`analysis`、`query` 截断至 200 字符 | ✅ 是 | `truncate_for_log()` 第 86‑91 行；日志条目构建第 237‑242 行 |
| R-06 调试代码清除 | 删除 `debug_idx` 及相关注释 | ✅ 是 | 第 172 行后无残留调试开关 |
| R-06 使用 logging 模块 | 安全事件使用 `logging.warning/error` 记录 | ✅ 是 | 第 44‑53 行配置 `logging`；多处调用 `logger.warning/error` |
| R-07 符号链接防御 | 写入前检查输出目录是否为符号链接 | ✅ 是 | `safe_write_log_entry()` 第 105‑115 行；主函数第 158‑163 行 |
| R-07 上下文管理器 | 使用 `with open()` 写入文件 | ✅ 是 | `load_data()` 第 128 行；`safe_write_log_entry()` 第 117 行 |
| R-03/R-04 禁止绕过 | 不强制关闭输入校验或关闭 AMIA | ✅ 是 | 未修改 `USE_AMIA` 逻辑，校验层前置 |
| 无第三方库 | 仅使用 Python 标准库 + 已有 `torch`/`PIL` | ✅ 是 | `import` 列表无新增第三方包 |
| 注释标注修复 | 每处修复均标注风险编号（R‑01~R‑08） | ✅ 是 | 代码中多处注释，如第 18、23、87、117 等 |

---

## 第二层审查：人工业务逻辑审查

### 认证与授权

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 连接/API 前是否有身份验证 | ❌ 无 | 该框架为本地测试脚本，无网络服务，无身份验证设计，属于测试工具固有特性 |
| 文件访问权限控制 | ⚠️ 部分 | 通过路径白名单（限制在 `data/` 目录）和扩展名检查（隐含在图片格式由 PIL 处理）做了访问控制 |
| 是否遵循最小权限原则 | ✅ 是 | 仅允许读取 `data/` 下的图片，仅允许写入 `results/` 目录（且检查符号链接） |

### 错误响应安全性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 错误信息是否向用户泄露内部路径 | ✅ 修复后已消除 | 路径校验失败抛出 `ValueError`，但日志中仅记录原始路径，不暴露真实服务器路径 |
| 异常是否被静默吞掉 | ✅ 修复后已消除 | 所有异常均被捕获并记录日志，部分操作继续（跳过样本），不静默 |
| 异常是否暴露系统内部信息（堆栈） | ⚠️ 轻微 | 日志记录的错误消息包含具体异常描述，但未打印完整堆栈；生产环境可关闭调试 |

### 数据存取安全性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 文件读取路径是否受控 | ✅ 是 | `safe_join_and_validate()` 强制限制在 `data/` 目录，阻止路径穿越 |
| 文件写入路径是否受控 | ✅ 是 | 输出目录 `results/` 检查符号链接，阻止覆盖外部敏感文件 |
| 写入文件是否存在覆盖风险 | ⚠️ 存在 | 以追加模式写入，若文件已存在则继续追加，不会覆盖，但重复运行会累积数据（属于设计选择） |
| 大文件/资源耗尽防护 | ✅ 是 | 像素、文件大小、提示长度、单条日志大小均有限制 |

### 代码健壮性与可维护性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 是否有多余调试代码残留 | ✅ 已清除 | 无 `debug_idx` 或注释掉的断点 |
| 常量是否语义化 | ✅ 是 | `MAX_IMAGE_PIXELS`, `MAX_IMAGE_FILE_SIZE` 等使用大写常量 |
| 关键函数是否有文档 | ✅ 是 | 每个函数均有 docstring 说明功能 |

---

## 总体审查结论

### 已修复的高危问题
- ✅ **R-01 路径遍历**：通过 `realpath` + 边界校验彻底封堵。
- ✅ **R-02 模块搜索路径污染**：移除硬编码相对路径，改用基于脚本位置的绝对路径并校验关键模块。
- ✅ **R-04 资源耗尽（DoS）**：添加图片像素、文件大小、提示长度、单条日志大小限制。
- ✅ **R-05 异常处理缺失**：细粒度异常捕获覆盖所有关键操作，并记录日志。
- ✅ **R-06 日志泄露**：对敏感字段进行截断脱敏，使用 `logging` 模块。

### 遗留可接受风险
- **无身份验证**：该测试脚本在隔离环境中运行，未启用网络服务，属于合理设计。
- **明文传输/模型加载**：与原始设计一致，未涉及网络传输，本次不整改。
- **边界检查细微瑕疵**：`safe_join_and_validate()` 的前缀检查在 `user_path=""` 时会误拒，但合法图片路径不为空，不影响安全性。

### 新引入问题
- **无**：未发现新引入的安全漏洞。

**审查结论：** 整改后代码满足项目约束文档中 R‑01 至 R‑08 的所有要求，核心安全风险已消除，代码可合并入主分支。

---


---
---

# 安全审查清单

> **审查对象：** `成员代码/fengyongjia/watermarkLSB.py`（整改后版本）  
> **审查人：** fengyongjia（冯永嘉）  
> **审查日期：** 2026-06-27

---

## 第一层审查：对照项目约束文档检查

> 验证 AI 生成代码是否真正落实了约束文档中 R‑01 ~ R‑06 的要求

| 约束条目（风险编号） | 具体要求 | 代码中是否实际落实 | 落实位置 |
|----------------------|----------|-------------------|----------|
| R-01 路径规范化 | 使用 `os.path.realpath()` 规范化所有文件路径 | ✅ 是 | `safe_validate_path()` 函数 |
| R-01 边界检查 | 验证规范化路径在安全目录内 | ✅ 是 | `safe_validate_path()` 中 `startswith` 检查 |
| R-01 消除硬编码路径 | 将 `'buptgray.bmp'` 等替换为可配置参数 | ✅ 是 | 主程序中通过环境变量 `LSB_INPUT`/`LSB_OUTPUT` 配置 |
| R-02 非空检查 | 拒绝空消息 | ✅ 是 | `validate_message()` 第 1 层检查 |
| R-02 长度限制 | 设置 `MAX_MESSAGE_LENGTH = 1024` | ✅ 是 | `validate_message()` 第 2 层检查 |
| R-02 字符集校验 | 仅允许可打印字符 + 常用中文 | ✅ 是 | `validate_message()` 逐字符 ord() 范围检查 |
| R-03 扩展名白名单 | 设置 `ALLOWED_EXTENSIONS` | ✅ 是 | 常量定义 + `validate_image_file()` 中校验 |
| R-03 文件大小限制 | 设置 `MAX_IMAGE_FILE_SIZE = 50MB` | ✅ 是 | `validate_image_file()` 中 `os.path.getsize()` 检查 |
| R-03 像素尺寸限制 | 设置 `MAX_IMAGE_PIXELS = 16MP` | ✅ 是 | `validate_image_pixels()` 函数 |
| R-04 具体异常类型 | 不使用裸 `except Exception` | ✅ 是 | `PIL.UnidentifiedImageError`、`FileNotFoundError`、`OSError`、`ValueError` 等 |
| R-04 logging 记录 | 安全事件使用 `logging` 模块 | ✅ 是 | `logger.error/warning/info` 贯穿全文 |
| R-05 移除硬编码种子 | 不再使用 `np.random.seed(2021)` | ✅ 是 | 替换为 `get_seed()` 函数 |
| R-05 环境变量支持 | 支持 `LSB_SEED` 环境变量 | ✅ 是 | `get_seed()` 优先读取 `LSB_SEED` |
| R-06 输出覆盖保护 | 文件存在时重命名而非覆盖 | ✅ 是 | `safe_output_path()` 中 `os.path.exists()` + 时间戳 |
| R-06 输出路径安全 | 输出路径也做规范化校验 | ✅ 是 | `safe_output_path()` 调用 `safe_validate_path()` |
| 核心算法不变 | LSB 次低有效位 + 随机位置逻辑不变 | ✅ 是 | `hide_message_improved_sequential()` 和 `extract_message_improved_sequential()` 核心逻辑完整保留 |
| 无第三方库 | 只用标准库 + numpy/PIL/cv2/matplotlib | ✅ 是 | import 清单无新增第三方包 |
| 注释标注修复 | 每处修复标注 R-01~R-06 | ✅ 是 | 代码中多处 `# R-01:`、`# R-02:` 等注释 |

---

## 第二层审查：人工业务逻辑审查

### 认证与授权

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 隐写操作前是否有密钥验证 | ⚠️ 部分 | 通过 `LSB_SEED` 环境变量实现基于种子的访问控制，但未做完整的密钥派生 |
| 文件访问权限控制 | ✅ 是 | `safe_validate_path()` 限制在 `SAFE_IMAGE_DIR` 和 `SAFE_OUTPUT_DIR` 内 |
| 是否遵循最小权限原则 | ✅ 是 | 图像读取限制在 `images/`，输出限制在 `output/` |

### 错误响应安全性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 错误信息是否泄露内部路径 | ✅ 已消除 | 日志仅打印文件名（`os.path.basename`），不暴露完整路径 |
| 异常是否被静默吞掉 | ✅ 已消除 | 所有异常均被捕获并记录日志 |
| 异常是否暴露敏感信息 | ⚠️ 轻微 | 错误消息包含异常描述，但不包含文件内容或消息原文 |

### 数据存取安全性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 文件读取路径受控 | ✅ 是 | `safe_validate_path()` 强制限制在 `images/` 目录 |
| 文件写入路径受控 | ✅ 是 | `safe_output_path()` 强制限制在 `output/` 目录 |
| 写入文件是否存在覆盖风险 | ✅ 已消除 | 文件存在时自动时间戳重命名 |

### 代码健壮性与可维护性

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 魔法数字是否消除 | ✅ 是 | `MAX_IMAGE_FILE_SIZE`、`MAX_IMAGE_PIXELS`、`MAX_MESSAGE_LENGTH` 等 |
| 关键函数文档 | ✅ 是 | 每个函数均有 docstring + 风险编号标注 |
| 调试代码残留 | ✅ 无 | 无遗留调试代码 |

---

## 总体审查结论

### 已修复的问题
- ✅ **R-01 路径硬编码与路径穿越**：通过 `realpath` + 安全目录边界校验彻底封堵。
- ✅ **R-02 输入消息未校验**：三重校验（非空 + 长度 + 字符集）。
- ✅ **R-03 文件格式/大小未校验**：扩展名白名单 + 50MB 限制 + 16MP 限制。
- ✅ **R-04 异常处理不完整**：6+ 种具体异常类型 + logging 记录。
- ✅ **R-05 固定随机种子**：环境变量 + 非确定性种子。
- ✅ **R-06 输出文件覆盖**：存在检查 + 时间戳重命名。

### 遗留可接受风险
- **种子管理依赖用户**：若用户不设置 `LSB_SEED` 环境变量，种子是非确定性的，提取时需要用户自行记录种子值。这是安全性的代价（避免硬编码），可通过文档说明引导用户。
- **无网络认证**：本脚本为本地实验工具，不涉及网络服务，无需额外认证。

### 新引入问题
- **无**：未发现新引入的安全漏洞。

**审查结论：** 整改后代码满足项目约束文档中 R‑01 至 R‑06 的所有要求，核心安全风险已消除，原有 LSB 算法功能完整保留，代码可合并入主分支。

---

# 安全审查清单

> **模块：** `成员代码/weichunru/DCT.py` — DCT 数字水印安全整改
> **审查时间：** 2026-06-27
> **审查人：** 位春汝（weichunru）

---

## 第一层审查：对照 Prompt 与约束文档逐条检查

### 1.1 R-01：API Key 身份认证

| 检查点 | 要求 | 结果 |
|--------|------|------|
| verify_api_key() 函数存在 | 必须新增 | ✅ |
| None 检查 | 拒绝 | ✅ |
| 空字符串检查 | 拒绝 | ✅ |
| 纯空白检查 | 拒绝 | ✅ |
| 无效 Key 检查 | 拒绝 | ✅ |
| 环境变量读取 | DCT_API_KEY | ✅ |
| 验证网关位置 | 水印嵌入前 | ✅ |
| 失败 exit(1) | 终止程序 | ✅ |
| 错误提示区分 | "未提供" vs "无效" | ✅ |

### 1.2 R-02：路径穿越防护

| 检查点 | 要求 | 结果 |
|--------|------|------|
| os.path.realpath() 规范化 | 所有文件路径 | ✅ |
| os.path.basename() 剥离目录 | 防止 ../ 穿越 | ✅ |
| 安全目录边界检查 | startswith 校验 | ✅ |
| 输入路径均经过 safe_resolve_path() | bupt.bmp, watermark.bmp | ✅ |
| 输出路径均经过 safe_resolve_path() | 3 个输出文件 | ✅ |

### 1.3 R-03：文件校验

| 检查点 | 要求 | 结果 |
|--------|------|------|
| 扩展名白名单 | {'.bmp'} | ✅ |
| 文件大小上限 | 50MB | ✅ |
| 文件存在性检查 | os.path.isfile() | ✅ |
| 校验在加载前执行 | validate_image_file() | ✅ |

### 1.4 R-04：异常处理

| 检查点 | 要求 | 结果 |
|--------|------|------|
| 具体异常类型 | FileNotFoundError, ValueError, OSError, cv2.error | ✅ |
| 无裸 except Exception | 禁止 | ✅ |
| 安全事件区分 | 不同异常不同提示 | ✅ |
| cv2.imread() 返回 None 检查 | 文件损坏检测 | ✅ |

### 1.5 DCT 算法完整性

| 检查点 | 结果 |
|--------|------|
| BLOCK_SHAPE / ALPHA / POS_A / POS_B | ✅ 未修改 |
| cv2.dct / cv2.idct 变换 | ✅ 未修改 |
| 水印嵌入/提取逻辑 | ✅ 未修改 |
| PSNR / NC 计算 | ✅ 未修改 |

### 1.6 第三方库检查

| 检查点 | 结果 |
|--------|------|
| import 清单仅含标准库 + numpy/cv2/matplotlib | ✅ |
| 无新增 pip 包 | ✅ |

### 1.7 禁止行为检查

| 检查点 | 结果 |
|--------|------|
| 未硬编码有效 Key | ✅ |
| 错误提示未泄露 Key | ✅ |
| 无 eval/exec | ✅ |
| 无 input() | ✅ |

---

## 第二层审查：人工安全审计

### 2.1 认证授权分层

| 检查项 | 结果 |
|--------|------|
| 认证与业务逻辑完全分离 | ✅ |
| 认证失败不进入任何业务代码 | ✅ |

### 2.2 最小权限原则

| 检查项 | 结果 |
|--------|------|
| 环境变量存储密钥 | ✅ |
| 文件操作限制在脚本目录内 | ✅ |
| 仅允许 .bmp 格式 | ✅ |

### 2.3 绕过可能性分析

| 绕过尝试 | 是否可行 |
|----------|----------|
| 不设环境变量 | ❌ 拒绝 |
| 设空/空白 Key | ❌ 拒绝 |
| 设无效 Key | ❌ 拒绝 |
| 路径穿越 `../` | ❌ basename 剥离 |
| 非 BMP 文件 | ❌ 扩展名白名单拒绝 |
| 超大文件 | ❌ 50MB 上限拒绝 |

### 2.4 错误响应安全

| 检查项 | 结果 |
|--------|------|
| 不含有效 Key 值 | ✅ |
| 不含系统路径（仅显示文件名） | ✅ |
| 区分不同错误类型 | ✅ |

---

## 审查总结

| 审查层 | 检查项数 | 通过 | 未通过 |
|--------|----------|------|--------|
| 第一层 | 32 | 32 | 0 |
| 第二层 | 12 | 12 | 0 |

**结论：** 修复后的 DCT.py 满足 R-01~R-04 全部安全约束要求，DCT 算法零修改，无第三方库引入，全部测试通过。

---

